const initialState = {
  value: 0
  /*cart: {
        content: []
        } esempio lezione*/
};

const mainReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mainReducer;
